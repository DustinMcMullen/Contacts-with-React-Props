import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

ReactDOM.render(<App />, document.getElementById("root"));

//1. Apply CSS styles to App.jsx component CHECK
//to match the appearance on the completed app: CHECK
//https://c6fkx.csb.app/ CHECK
//2. Extract the contact card as a reusable Card component. CHECK
//3. Use props to render the default Beyonce contact card CHECK
//so the Card component can be reused for other contacts.CHECK
//4. Import the contacts.js file to create card components. CHECK
